import { StandardButton } from "std-widgets.slint";

export component MessageBox inherits Dialog {
    
    property <string> dialog_text: "DialogTest";

    always-on-top: true;

    Text {
        text: dialog_text;
        horizontal-alignment: center;
        vertical-alignment: center;
    }

    StandardButton { kind: ok; }
}